Help on class ClobClient in module py_clob_client.client:

class ClobClient(builtins.object)
 |  ClobClient(host, chain_id: int = None, key: str = None, creds: py_clob_client.clob_types.ApiCreds = None, signature_type: int = None, funder: str = None, builder_config: py_builder_signing_sdk.config.BuilderConfig = None, tick_size_ttl: float = 300.0)
 |
 |  Methods defined here:
 |
 |  __init__(self, host, chain_id: int = None, key: str = None, creds: py_clob_client.clob_types.ApiCreds = None, signature_type: int = None, funder: str = None, builder_config: py_builder_signing_sdk.config.BuilderConfig = None, tick_size_ttl: float = 300.0)
 |      Initializes the clob client
 |      The client can be started in 3 modes:
 |      1) Level 0: Requires only the clob host url
 |                  Allows access to open CLOB endpoints
 |
 |      2) Level 1: Requires the host, chain_id and a private key.
 |                  Allows access to L1 authenticated endpoints + all unauthenticated endpoints
 |
 |      3) Level 2: Requires the host, chain_id, a private key, and Credentials.
 |                  Allows access to all endpoints
 |
 |  are_orders_scoring(self, params: py_clob_client.clob_types.OrdersScoringParams)
 |      Check if the orders are currently scoring
 |      Requires Level 2 authentication
 |
 |  assert_builder_auth(self)
 |      Builder Auth
 |
 |  assert_level_1_auth(self)
 |      Level 1 Poly Auth
 |
 |  assert_level_2_auth(self)
 |      Level 2 Poly Auth
 |
 |  calculate_market_price(self, token_id: str, side: str, amount: float, order_type: py_clob_client.clob_types.OrderType) -> float
 |      Calculates the matching price considering an amount and the current orderbook
 |
 |  can_builder_auth(self) -> bool
 |
 |  cancel(self, order_id)
 |      Cancels an order
 |      Level 2 Auth required
 |
 |  cancel_all(self)
 |      Cancels all available orders for the user
 |      Level 2 Auth required
 |
 |  cancel_market_orders(self, market: str = '', asset_id: str = '')
 |      Cancels orders
 |      Level 2 Auth required
 |
 |  cancel_orders(self, order_ids)
 |      Cancels orders
 |      Level 2 Auth required
 |
 |  clear_tick_size_cache(self, token_id: str = None)
 |      Clears the tick size cache, forcing fresh fetches on the next access.
 |
 |      Args:
 |          token_id: If provided, only clears the cache for this token.
 |                    Otherwise clears all cached tick sizes.
 |
 |  create_and_post_order(self, order_args: py_clob_client.clob_types.OrderArgs, options: py_clob_client.clob_types.PartialCreateOrderOptions = None)
 |      Utility function to create and publish an order
 |
 |  create_api_key(self, nonce: int = None) -> py_clob_client.clob_types.ApiCreds
 |      Creates a new CLOB API key for the given
 |
 |  create_market_order(self, order_args: py_clob_client.clob_types.MarketOrderArgs, options: Optional[py_clob_client.clob_types.PartialCreateOrderOptions] = None)
 |      Creates and signs an order
 |      Level 1 Auth required
 |
 |  create_or_derive_api_creds(self, nonce: int = None) -> py_clob_client.clob_types.ApiCreds
 |      Creates API creds if not already created for nonce, otherwise derives them
 |
 |  create_order(self, order_args: py_clob_client.clob_types.OrderArgs, options: Optional[py_clob_client.clob_types.PartialCreateOrderOptions] = None)
 |      Creates and signs an order
 |      Level 1 Auth required
 |
 |  create_readonly_api_key(self) -> py_clob_client.clob_types.ReadonlyApiKeyResponse
 |      Creates a new readonly API key for a user
 |      Level 2 Auth required
 |
 |  delete_api_key(self)
 |      Deletes an API key
 |      Level 2 Auth required
 |
 |  delete_readonly_api_key(self, key: str) -> bool
 |      Deletes a readonly API key for a user
 |      Level 2 Auth required
 |
 |  derive_api_key(self, nonce: int = None) -> py_clob_client.clob_types.ApiCreds
 |      Derives an already existing CLOB API key for the given address and nonce
 |
 |  drop_notifications(self, params: py_clob_client.clob_types.DropNotificationParams = None)
 |      Drops the notifications for a user
 |      Requires Level 2 authentication
 |
 |  get_address(self)
 |      Returns the public address of the signer
 |
 |  get_api_keys(self)
 |      Gets the available API keys for this address
 |      Level 2 Auth required
 |
 |  get_balance_allowance(self, params: py_clob_client.clob_types.BalanceAllowanceParams = None)
 |      Fetches the balance & allowance for a user
 |      Requires Level 2 authentication
 |
 |  get_builder_trades(self, params: py_clob_client.clob_types.TradeParams = None, next_cursor='MA==')
 |      Get trades originated by the builder
 |
 |  get_closed_only_mode(self)
 |      Gets the closed only mode flag for thsi address
 |      Level 2 Auth required
 |
 |  get_collateral_address(self)
 |      Returns the collateral token address
 |
 |  get_conditional_address(self)
 |      Returns the conditional token address
 |
 |  get_exchange_address(self, neg_risk=False)
 |      Returns the exchange address
 |
 |  get_fee_rate_bps(self, token_id: str) -> int
 |
 |  get_last_trade_price(self, token_id)
 |      Fetches the last trade price token_id
 |
 |  get_last_trades_prices(self, params: list[py_clob_client.clob_types.BookParams])
 |      Fetches the last trades prices for a set of token ids
 |
 |  get_market(self, condition_id)
 |      Get a market by condition_id
 |
 |  get_market_trades_events(self, condition_id)
 |      Get the market's trades events by condition id
 |
 |  get_markets(self, next_cursor='MA==')
 |      Get the current markets
 |
 |  get_midpoint(self, token_id)
 |      Get the mid market price for the given market
 |
 |  get_midpoints(self, params: list[py_clob_client.clob_types.BookParams])
 |      Get the mid market prices for a set of token ids
 |
 |  get_neg_risk(self, token_id: str) -> bool
 |
 |  get_notifications(self)
 |      Fetches the notifications for a user
 |      Requires Level 2 authentication
 |
 |  get_ok(self)
 |      Health check: Confirms that the server is up
 |      Does not need authentication
 |
 |  get_order(self, order_id)
 |      Fetches the order corresponding to the order_id
 |      Requires Level 2 authentication
 |
 |  get_order_book(self, token_id) -> py_clob_client.clob_types.OrderBookSummary
 |      Fetches the orderbook for the token_id
 |
 |  get_order_book_hash(self, orderbook: py_clob_client.clob_types.OrderBookSummary) -> str
 |      Calculates the hash for the given orderbook
 |
 |  get_order_books(self, params: list[py_clob_client.clob_types.BookParams]) -> list[py_clob_client.clob_types.OrderBookSummary]
 |      Fetches the orderbook for a set of token ids
 |
 |  get_orders(self, params: py_clob_client.clob_types.OpenOrderParams = None, next_cursor='MA==')
 |      Gets orders for the API key
 |      Requires Level 2 authentication
 |
 |  get_price(self, token_id, side)
 |      Get the market price for the given market
 |
 |  get_prices(self, params: list[py_clob_client.clob_types.BookParams])
 |      Get the market prices for a set
 |
 |  get_readonly_api_keys(self) -> list[str]
 |      Gets the available readonly API keys for this address
 |      Level 2 Auth required
 |
 |  get_sampling_markets(self, next_cursor='MA==')
 |      Get the current sampling markets
 |
 |  get_sampling_simplified_markets(self, next_cursor='MA==')
 |      Get the current sampling simplified markets
 |
 |  get_server_time(self)
 |      Returns the current timestamp on the server
 |      Does not need authentication
 |
 |  get_simplified_markets(self, next_cursor='MA==')
 |      Get the current simplified markets
 |
 |  get_spread(self, token_id)
 |      Get the spread for the given market
 |
 |  get_spreads(self, params: list[py_clob_client.clob_types.BookParams])
 |      Get the spreads for a set of token ids
 |
 |  get_tick_size(self, token_id: str) -> Literal['0.1', '0.01', '0.001', '0.0001']
 |
 |  get_trades(self, params: py_clob_client.clob_types.TradeParams = None, next_cursor='MA==')
 |      Fetches the trade history for a user
 |      Requires Level 2 authentication
 |
 |  is_order_scoring(self, params: py_clob_client.clob_types.OrderScoringParams)
 |      Check if the order is currently scoring
 |      Requires Level 2 authentication
 |
 |  post_heartbeat(self, heartbeat_id: Optional[str])
 |      Sends a heartbeat to the server, if heartbeats are started and one isn't sent within 10s, all orders will be cancelled
 |      Requires Level 2 authentication
 |
 |  post_order(self, order, orderType: py_clob_client.clob_types.OrderType = 'GTC', post_only: bool = False)
 |      Posts the order
 |
 |  post_orders(self, args: list[py_clob_client.clob_types.PostOrdersArgs])
 |      Posts orders
 |
 |  set_api_creds(self, creds: py_clob_client.clob_types.ApiCreds)
 |      Sets client api creds
 |
 |  update_balance_allowance(self, params: py_clob_client.clob_types.BalanceAllowanceParams = None)
 |      Updates the balance & allowance for a user
 |      Requires Level 2 authentication
 |
 |  validate_readonly_api_key(self, address: str, key: str) -> str
 |      Validates a readonly API key for a given address
 |      This is a public endpoint, no authentication required
 |
 |  ----------------------------------------------------------------------
 |  Data descriptors defined here:
 |
 |  __dict__
 |      dictionary for instance variables
 |
 |  __weakref__
 |      list of weak references to the object
